name: Release Check

on:
  push:
    branches: [main, master]

permissions:
  contents: read

jobs:
  release-check:
    name: Run full release checks
    uses: ./.github/workflows/shared-checks.yml
    with:
      make-target: release-check
